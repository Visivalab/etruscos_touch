npm run build construye aquí el main.js y el style.css necesarios, pero
hay que copiar a mano: 
. index.html - y borrar el type="module" del script
. la carpeta assets entera

Así que si hay un cambio en los assets o en el index, recordar copiarlos. 
Si son cambios de js y css, con el build es suficiente